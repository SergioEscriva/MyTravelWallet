<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="ExtraText"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />


            </intent-filter>
        </activity>
        <activity android:name=".activities.EditarTransaccionesActivity" />
        <activity android:name=".activities.AgregarTransaccionActivity" />
        <activity android:name=".activities.ListarTransaccionesActivity" />
        <activity android:name=".activities.ListarWalletsActivity" />
        <activity android:name=".activities.AgregarWalletActivity" />
        <activity android:name=".activities.EditarWalletActivity" />
        <activity android:name=".activities.ResolverDeudaActivity" />
        <activity android:name=".utilities.PopUpPagadorActivity" />
        <activity android:name=".ayuda.ListaWalletsAyuda" />
        <activity android:name=".ayuda.SaldarDeudasAyuda" />
        <activity android:name=".ayuda.EditarWalletAyuda" />
        <activity android:name=".ayuda.ListaTransaccionesAyuda" />
    </application>

</manifest>